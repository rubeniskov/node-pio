{
    "ci-test": [
        "karma:ci-unit",
        "protractor:ci-e2e"
    ],
    "live": "concurrent:live",
    "build": "concurrent:build",
    "build:app:css" : "sass:build",
    "build:app:js" : ["jshint:core-js", "requirejs:core-js"]
}
