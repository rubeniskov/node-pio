{
    "ci-test": [
        "requirejs:ci-unit",
        "karma:ci-unit"
    ],
    "live": "concurrent:live",
    "build": "concurrent:build",
    "build:app:css" : "sass:build",
    "build:app:js" : ["jshint:build", "requirejs:build"]
}
