module.exports = function(grunt, data) {
    return {
        "watch": {
            options: {
                title: 'Task Complete',
                message: '<%= app.name %> building finished running',
            }
        }
    };
};
